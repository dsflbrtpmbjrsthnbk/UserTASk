# 🚀 ДЕПЛОЙ НА RENDER - Пошаговая инструкция

## 📋 Что нужно сделать

1. Загрузить проект на GitHub
2. Создать PostgreSQL базу данных на Render
3. Создать Web Service на Render
4. Настроить переменные окружения
5. Задеплоить!

---

## 🎯 ШАГ 1: Загрузить проект на GitHub

### 1.1 Создайте репозиторий на GitHub

1. Зайдите на https://github.com
2. Нажмите "New repository"
3. Название: `user-management-app`
4. Приватность: Public или Private
5. Нажмите "Create repository"

### 1.2 Загрузите проект

Откройте терминал в папке проекта:

```bash
# Инициализируйте git
git init

# Добавьте .gitignore
echo "bin/
obj/
.vs/
.vscode/
*.user
*.suo
appsettings.Development.json
Migrations/" > .gitignore

# Добавьте все файлы
git add .

# Сделайте коммит
git commit -m "Initial commit"

# Подключите к GitHub (замените YOUR_USERNAME и YOUR_REPO)
git remote add origin https://github.com/YOUR_USERNAME/user-management-app.git

# Загрузите код
git branch -M main
git push -u origin main
```

---

## 🎯 ШАГ 2: Создайте аккаунт на Render

1. Зайдите на https://render.com
2. Нажмите "Get Started for Free"
3. Зарегистрируйтесь через GitHub

---

## 🎯 ШАГ 3: Создайте PostgreSQL базу данных

1. В Render Dashboard нажмите "New +"
2. Выберите "PostgreSQL"
3. Настройки:
   - **Name:** `usermanagement-db`
   - **Database:** `usermanagement`
   - **User:** `usermanagement`
   - **Region:** выберите ближайший
   - **Plan:** Free
4. Нажмите "Create Database"
5. **ВАЖНО:** Скопируйте "Internal Database URL" - он понадобится!

---

## 🎯 ШАГ 4: Создайте Web Service

1. В Render Dashboard нажмите "New +"
2. Выберите "Web Service"
3. Подключите ваш GitHub репозиторий
4. Настройки:
   - **Name:** `user-management-app`
   - **Region:** тот же что и база данных
   - **Branch:** `main`
   - **Runtime:** `Docker`
   - **Plan:** Free

5. **Environment Variables** (переменные окружения):
   
   Нажмите "Add Environment Variable" и добавьте:

   | Key | Value |
   |-----|-------|
   | `ASPNETCORE_ENVIRONMENT` | `Production` |
   | `DATABASE_URL` | (вставьте Internal Database URL из шага 3) |
   | `Email__SmtpServer` | `smtp.gmail.com` |
   | `Email__SmtpPort` | `587` |
   | `Email__SenderEmail` | `your-email@gmail.com` |
   | `Email__SenderPassword` | `your-app-password` |
   | `Email__SenderName` | `User Management System` |

   **ВАЖНО:** Для Email используйте Gmail App Password!

6. Нажмите "Create Web Service"

---

## 🎯 ШАГ 5: Дождитесь деплоя

Render автоматически:
1. Склонирует ваш репозиторий
2. Соберет Docker образ
3. Создаст таблицы в базе данных (автоматическая миграция)
4. Запустит приложение

Это займет 5-10 минут.

---

## ✅ ШАГ 6: Проверьте что работает

1. После деплоя Render покажет URL вашего приложения
2. Например: `https://user-management-app.onrender.com`
3. Откройте его в браузере
4. Зарегистрируйтесь и войдите!

---

## 🔧 Полезные команды

### Просмотр логов

В Render Dashboard → ваш сервис → вкладка "Logs"

### Перезапуск приложения

В Render Dashboard → ваш сервис → "Manual Deploy" → "Deploy latest commit"

### Проверка базы данных

В Render Dashboard → PostgreSQL → "Connect" → копируйте команду подключения

---

## 🐛 Решение проблем

### Ошибка: "Application failed to start"

**Проверьте логи:**
- В Render Dashboard → Logs
- Ищите ошибки миграции БД или подключения

**Частые причины:**
- Неправильный DATABASE_URL
- База данных не создана
- Ошибка в коде

### Ошибка: "Cannot connect to database"

**Проверьте:**
- DATABASE_URL правильно скопирован
- База данных создана
- Используется Internal Database URL (не External)

### Приложение работает локально, но не на Render

**Проверьте:**
- Все ли файлы загружены на GitHub
- Правильно ли настроен Dockerfile
- Нет ли ошибок в логах Render

---

## 📚 Дополнительно

### Как обновить приложение

1. Внесите изменения в код
2. Закоммитьте:
   ```bash
   git add .
   git commit -m "Update"
   git push
   ```
3. Render автоматически передеплоит!

### Как настроить свой домен

1. В Render Dashboard → ваш сервис → Settings → Custom Domains
2. Добавьте ваш домен
3. Настройте DNS записи

### Как масштабировать

На бесплатном плане:
- Автоматический sleep после 15 минут неактивности
- Первый запрос может быть медленным

Платные планы:
- Без sleep
- Больше памяти
- Быстрее

---

## ✅ Готово!

Теперь ваше приложение доступно в интернете!

**URL:** https://ваше-имя-app.onrender.com

Можете делиться ссылкой с преподавателем! 🎉
